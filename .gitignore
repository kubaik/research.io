# ── NEVER COMMIT ─────────────────────────────────────────────────
.env
.env.*
*.pyc
__pycache__/
.secrets_cache.json

# Build artifacts (CI uses _site/, local dev uses docs/)
# docs/ is intentionally committed — it IS the deployable static site.
# _site/ is only used inside GitHub Actions and is never committed.
_site/